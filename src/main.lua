-- @include lib\examplefile.lua

--@if NODE_ENV='dev'
    --running in dev mode
-- @endif

-- @if NODE_ENV!='dev'
    --running in production mode
-- @endif

function init()
    
end

function tick()
    
end

function draw()
end

function update()
end